source("renv/activate.R")
options(repos = c(CRAN = "https://cloud.r-project.org/", PlanetExpressShip = "https://minicran.planetexpressship.com"))
# options(renv.download.headers = function(url) {
#   if (grepl("^https://minicran.planetexpressship.com", url))
#     return(c(Authorization = Sys.getenv("AUTH_HEADER")))
# })
